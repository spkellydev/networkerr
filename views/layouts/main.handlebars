<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    {{!-- <link rel="stylesheet" href="https://gitcdn.link/repo/Chalarangelo/mini.css/master/dist/mini-default.min.css"> --}}
    <link rel="stylesheet" href="https://gitcdn.link/repo/Chalarangelo/mini.css/master/dist/mini-pwa.min.css">

    <link rel="stylesheet" href="../../static/css/style.css">
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <title>{{{ title }}}</title>
</head>
<body>
    <header class="sticky">
        <a href="/" class="logo">Networkerr</a>
        {{#if user}}
            <a href="/users/profile/{{user.id}}" role="button">My Profile</a>
            <a href="/users/logout" role="button">Logout</a>
        {{else}}
            <a href="/users/login" role="button">Login</a><span>|</span>
            <a href="/users/register" role="button">Register</a>
        {{/if}}
    </header>
 
     <div class="row text-center">
        <div class="col-lg-12">
          {{#if success_msg}}
            <mark class="alert tertiary dismissable">{{success_msg}}</mark>
          {{/if}}

          {{#if error_msg}}
            <mark class="alert secondary dismissable">{{error_msg}}</mark>
          {{/if}}

          {{#if error}}
            <mark class="alert dismissable">{{error}}</mark>
          {{/if}}
        </div>
    </div>

     {{{body}}}


    <footer class="text-center">&copy; 2017 - Sean Kelly</footer>
    {{!-- <script src="../../static/js/main.js"></script> --}}
</body>
</html>